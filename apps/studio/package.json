{
  "name": "studio",
  "version": "0.0.0",
  "scripts": {
    "dev": "drizzle-kit studio --config ../../packages/database/drizzle.config.ts --port 3005",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.5"
  }
}
